# Ralph Progress Log
Started: Fri Mar 6 2026
---

## Codebase Patterns
- Use Drizzle ORM with `drizzle-orm/libsql` for Turso database access
- SQLite stores JSON as text — use `text('field', { mode: 'json' }).$type<T>()` for type-safe JSON columns
- Use `crypto.randomUUID()` for generating primary keys in schema defaults
- Environment variables for Turso: TURSO_DATABASE_URL and TURSO_AUTH_TOKEN
- NextAuth v5: Config in `src/auth.ts`, use `auth()` server-side, `useSession()` client-side
- Protected routes: Middleware redirects pages to sign-in, returns 401 for API routes
- Leaflet MUST be dynamically imported with next/dynamic ssr:false via a wrapper client component
- Use `and()` from drizzle-orm to combine multiple where conditions
- Upsert pattern: `.insert().onConflictDoUpdate({ target: [col1, col2], set: {...} })`
- Generate migrations with `npx drizzle-kit generate`
- shadcn/ui components are in src/components/ui/ — do not modify their internals unless fixing actual bugs
- Site data model: `launch_sites` (imported), `custom_sites` (user-created), legacy `sites` table
- `forecasts_cache` uses siteType discriminator ('launch'|'custom'|'legacy') — always include in queries/inserts
- Complex migrations: Use custom migration scripts, not just drizzle-kit push (it compares schema, not migrations)
- Lazy DB imports in utilities: Use `await import('@/db')` inside functions to avoid requiring env vars at module load (allows testing without DB)
- CLI scripts: Add `import 'dotenv/config'` at top to load .env before accessing process.env
- Drizzle timestamp columns expect Date objects, not numbers (use `new Date()` not `Date.now()`)
- Vitest config: Exclude e2e folder to prevent Playwright tests from running in vitest
---

## 2026-03-06 17:48 - US-001
- Implemented code quality tooling foundation for Phase 1
- Files changed:
  - Created: .prettierrc, .prettierignore, vitest.config.ts, playwright.config.ts, .husky/pre-commit, e2e/homepage.spec.ts, src/lib/__tests__/scoring.test.ts
  - Modified: package.json (added test scripts), 74 files formatted with Prettier
- Fixed all 18 ESLint errors:
  - Replaced `any` with proper types: `unknown` for error handlers, `SiteFormData` for form data
  - Fixed unescaped apostrophes in JSX (replaced with &apos;)
  - Removed unused imports (sql from drizzle-orm, Badge from score-cell, error param in dashboard-client)
  - Fixed impure Math.random() call in sidebar skeleton component (used useState initializer)
- **Learnings for future iterations:**
  - Prettier formatting applies to entire codebase - run it last to avoid merge conflicts
  - SiteFormData type doesn't include createdAt/updatedAt (form doesn't handle those)
  - Error type narrowing in TypeScript: check `typeof error === 'object'` and `'statusCode' in error` before accessing properties
  - React purity rule: use `useState(() => Math.random())` instead of `useMemo` to avoid impure functions in render
  - Pre-commit hooks now enforce code quality automatically
---

## 2026-03-06 18:05 - US-002
- Implemented database schema for launch sites, custom sites, and user favorites
- Files changed:
  - Created: drizzle/0003_nebulous_captain_universe.sql, scripts/run-migration.ts, scripts/complete-migration.ts
  - Modified: src/db/schema.ts (added 3 new tables), src/lib/weather.ts (updated for new schema)
- Database changes:
  - Created `launch_sites` table for imported sites (23 fields including slug, orientations, landing info)
  - Created `user_favorite_sites` junction table with custom overrides (max_wind, ideal_directions, notify)
  - Created `custom_sites` table for user-created monitoring points
  - Modified `forecasts_cache` to use site_type discriminator ('launch'|'custom'|'legacy') instead of FK
  - Migrated existing user sites from `sites` table to `custom_sites` (1 site migrated)
  - Kept legacy `sites` table for backward compatibility
- **Learnings for future iterations:**
  - drizzle-kit push compares schema to DB state, not migration files - use custom migration scripts for complex data migrations
  - Use site_type discriminator pattern instead of foreign keys when a table needs to reference multiple source tables
  - Migration script pattern: split by `--> statement-breakpoint` to execute statements sequentially
  - Always verify data migration with SELECT COUNT(*) queries after migration
  - Use `IF NOT EXISTS` in manual migrations to allow safe re-runs
  - The forecasts_cache now needs siteType in all queries: `and(eq(siteId, id), eq(siteType, 'legacy'))`
---
## 2026-03-06 18:07 - US-003
- Implemented ParaglidingEarth site scraper with full Washington State import
- Files changed:
  - Created: src/lib/scraper/paraglidingearth.ts (scraper service with XML parsing)
  - Created: src/lib/scraper/__tests__/paraglidingearth.test.ts (18 unit tests, all passing)
  - Created: scripts/import-sites.ts (CLI script with --region and --bbox support)
  - Created: drizzle/0004_bouncy_timeslip.sql (added unique constraint on source_id)
  - Modified: package.json (added fast-xml-parser dependency)
  - Modified: vitest.config.ts (excluded e2e tests from vitest)
  - Modified: src/db/schema.ts (added .unique() to sourceId field)
- Implementation details:
  - XML parsing with fast-xml-parser (lightweight, no dependencies)
  - Orientation mapping: compass directions rated 2 (good) → ideal wind directions in degrees
  - Slug generation: lowercase, remove special chars, replace spaces with hyphens
  - Upsert pattern on source_id prevents duplicates on re-import
  - Rate limiting: 1 second delay before each API request
  - Successfully imported 55 sites from Washington State
- **Learnings for future iterations:**
  - Lazy import pattern for db: use `await import('@/db')` inside functions to avoid requiring env vars at module load time (enables testing utility functions without DB connection)
  - Fast-xml-parser options: `ignoreAttributes: false` to preserve XML attributes, `parseTagValue: true` to auto-convert numbers
  - ParaglidingEarth doesn't provide stable IDs - generated source_id from lat_lng coordinates for uniqueness
  - Handle both array and single-item XML responses: `Array.isArray(parsed.search.takeoff) ? ... : [...]`
  - Drizzle timestamp columns expect Date objects, not milliseconds (use `new Date()` not `Date.now()`)
  - Unique constraints must exist on conflict target columns - needed migration to add missing index
  - Vitest exclude config: add e2e folder to prevent Playwright tests from running in vitest
  - CLI scripts need `import 'dotenv/config'` at the top to load .env file before importing modules that access process.env
---

