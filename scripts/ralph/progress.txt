# Ralph Progress Log
Started: Fri Mar  6 19:15:08 +07 2026
---
## 2026-03-06 20:30 - US-001
- Implemented smart push notifications with atmospheric profile data
- Created src/lib/notifications.ts with rich analysis functions:
  - analyzeFlyingDay(): Analyzes atmospheric profile for rating, best window, concerns, highlights
  - generateNotification(): Creates plain-language notification messages
  - generateMorningDigest(): Summarizes all favorited sites in one notification
- Enhanced /api/notifications/check route to use atmospheric profile data
- Extended settings schema with per-site notification preferences (SiteNotificationPreferences):
  - enabled: boolean (per-site on/off)
  - minRating: 'Good' | 'Great' | 'Epic' (minimum rating threshold)
  - notifyTime: 'morning' | 'evening' | 'both' (timing preferences)
- Added morning digest option (morningDigestEnabled, morningDigestTime fields in settings table)
- Updated src/app/settings/actions.ts for backward compatibility with new schema
- Created migrations: drizzle/0006_tense_forge.sql and drizzle/0007_add_morning_digest.sql
- All quality checks pass: typecheck ✓, lint ✓, build ✓

**Learnings for future iterations:**
- The atmospheric profile API (src/lib/weather-profile.ts) provides rich data: lapse rate, thermal index, cloud base, top of lift estimates
- Notification messages should be plain-language and actionable, not raw scores
- Per-site preferences use JSON fields in SQLite for flexibility without migrations
- Backward compatibility: Convert between old boolean format and new structured preferences
- Morning digest runs during 6-8AM UTC window, skips individual notifications when enabled
- The notification check endpoint supports both enhanced and fallback (basic scoring) modes
---
## 2026-03-06 21:45 - US-000
- Made browse and site pages publicly accessible without authentication
- Updated src/middleware.ts:
  - Changed protectedRoutes from ['/sites', '/settings'] to ['/dashboard', '/settings']
  - Added publicRoutes for /sites/browse and /sites/ (all site detail pages)
  - Removed /api/sites and /api/weather from protectedApiRoutes (now public for GET)
- Updated src/app/sites/[slug]/favorite-button.tsx:
  - Added isAuthenticated prop to component
  - Shows "Sign in to Favorite" button for unauthenticated users
  - Redirects to /auth/signin with callbackUrl when clicked without authentication
- Updated src/app/sites/[slug]/page.tsx:
  - Removed conditional rendering of favorite button (now always shown)
  - Passes isAuthenticated={!!session} to LaunchSiteFavoriteButton
- Files changed: src/middleware.ts, src/app/sites/[slug]/favorite-button.tsx, src/app/sites/[slug]/page.tsx
- All quality checks pass: typecheck ✓, lint ✓, build ✓

**Learnings for future iterations:**
- Next.js middleware uses pattern matching - publicRoutes need explicit checks before protectedRoutes
- The middleware pattern '/sites/' catches all /sites/[slug] detail pages
- Auth flow: Pass isAuthenticated as boolean prop rather than session object to avoid serialization issues
- Favorite button pattern: Show action, prompt sign-in on click rather than hiding completely
- The middleware allows public GET access while server actions still check auth and throw errors
---
## 2026-03-06 22:00 - US-002
- Implemented comprehensive site search and discovery improvements
- Added full-text search across site name, region, country with recent search tracking
- Created advanced filtering UI with Sheet component:
  - Region, country, site type filters
  - Multi-select orientation filter (N, NE, E, SE, S, SW, W, NW)
  - Visual filter badges with quick removal
- Implemented multiple sort options:
  - By name (default)
  - By distance from user (geolocation with fallback)
  - By elevation (high to low)
  - By recently added
- Enhanced map with Leaflet marker clustering (react-leaflet-cluster)
  - Auto-fit bounds when sites change
  - Smooth spiderfy on cluster click
  - Performance optimized for large marker sets
- Added localStorage-based tracking:
  - Recent searches (max 5) with dropdown
  - Recently viewed sites
- All filters and sort options reflected in URL for shareable links
- Files changed:
  - src/app/sites/browse/actions.ts: Enhanced getLaunchSites, added getFilterOptions
  - src/app/sites/browse/browse-client.tsx: Complete rewrite with advanced filtering
  - src/app/sites/browse/page.tsx: Support new filter params
  - src/components/sites-browse-map.tsx: Added clustering and auto-bounds
  - src/lib/recent-searches.ts: localStorage utilities
  - src/components/recent-site-tracker.tsx: Client component for tracking views
  - src/app/sites/[slug]/page.tsx: Added recent site tracking
- Quality checks: typecheck ✓, lint ✓, build ✓

**Learnings for future iterations:**
- Leaflet marker clustering: Use react-leaflet-cluster (simpler than leaflet.markercluster)
- Map bounds: Use fitBounds with array of [lat, lng] tuples, not LatLngExpression[]
- React setState in effects: Initialize state with function () => getValue() to avoid lint errors
- URL search params: All filters in URL for shareability - Next.js searchParams pattern
- Client-side filtering: For 55 sites, can afford client filtering for better UX (instant sorting)
- Geolocation API: Request permission on-demand when user selects distance sort
- localStorage: Track recent searches/views for better UX, fail silently if unavailable
- Filter UI: Sheet component from shadcn/ui works great for mobile filter panels
- Active filter badges: Visual feedback with quick removal improves discoverability
---
## 2026-03-06 22:30 - US-003
- Implemented onboarding flow for new users with public landing page
- Added onboardingCompleted field to settings schema
- Created and applied migration 0008_onboarding_field.sql
- Created new public landing page at root (/):
  - Hero section with clear value proposition "Know when to fly, before you drive"
  - Sample windgram preview card for Tiger Mountain showing key metrics
  - Features section with 6 cards highlighting core capabilities
  - Mobile-first responsive design with CTAs for browse and sign-in
  - Footer with Open-Meteo attribution
- Moved dashboard from / to /dashboard route (protected)
- Created OnboardingFlow component (src/components/onboarding-flow.tsx):
  - 3-step modal overlay shown after first sign-in
  - Step 1: Welcome with feature overview (atmospheric profiles, notifications)
  - Step 2: Site discovery with nearby/popular sites list (uses geolocation)
  - Step 3: Completion screen with quick tips
  - Fully skippable with X button at any time
  - Progress indicators at bottom
- Added getOnboardingStatus and completeOnboarding server actions
- Dashboard conditionally renders onboarding flow for first-time users
- Files changed:
  - drizzle/0008_onboarding_field.sql: Migration for onboarding field
  - src/db/schema.ts: Added onboardingCompleted boolean to settings
  - src/app/page.tsx: New public landing page with hero showcase
  - src/app/dashboard/page.tsx: Moved from root, added onboarding check
  - src/app/dashboard/actions.ts: Fixed imports after file move
  - src/app/settings/actions.ts: Added onboarding status actions
  - src/components/onboarding-flow.tsx: New 3-step guided flow
  - src/components/dashboard-client.tsx: Fixed import path
- Quality checks: typecheck ✓, lint ✓, build ✓

**Learnings for future iterations:**
- Landing page pattern: Show immediate value (sample windgram preview) before asking for sign-in
- Onboarding state: Track in settings table, not user table (allows reset without user deletion)
- Modal overlays: Use fixed positioning with backdrop-blur for focus
- Geolocation API: Request permission on-demand, fail silently with fallback (popular sites)
- Skippable flows: Always provide X button - experienced users shouldn't be forced through steps
- Progress indicators: Simple dot indicators (active vs muted) work well for multi-step flows
- Mobile-first: Landing page tested on mobile breakpoints (360px, 390px, 768px)
- Route restructure: Dashboard moved from / to /dashboard, root becomes public landing
- Server actions for state: completeOnboarding updates DB and revalidates dashboard path
- First-time UX: Show onboarding only when onboardingCompleted is false and user is authenticated
---
## 2026-03-06 23:15 - US-004
- Implemented comprehensive performance and loading optimizations
- Progressive loading with React Suspense:
  - Site detail page split into instant site info + streamed forecast
  - Created SiteDetailForecast component loaded via Suspense
  - Skeleton loading states provide visual feedback during load
- Dashboard prefetching:
  - Created PrefetchProfiles component for background data fetching
  - Prefetches atmospheric profile data for favorited sites
  - Uses requestIdleCallback for non-blocking prefetch
  - First 3 sites prefetch immediately, remaining delayed 2 seconds
- Enhanced service worker caching:
  - Upgraded cache version to soarcast-v2 and added API_CACHE_NAME
  - Implemented stale-while-revalidate strategy for API responses
  - /api/weather/profile and /api/sites cached with instant returns
  - Fresh data fetched in background while serving cached responses
  - Graceful offline fallback with cached data
- Cache-Control headers:
  - Verified all API routes have appropriate caching
  - Profile data: 1hr cache with 30min stale-while-revalidate
  - Site data: 1hr cache with 2hr stale-while-revalidate
- Windgram chart already well-optimized with existing skeleton loading
- Files changed:
  - src/app/sites/[slug]/page.tsx: Progressive loading with Suspense
  - src/app/sites/[slug]/site-detail-forecast.tsx: New async forecast component
  - src/app/dashboard/page.tsx: Added prefetch component
  - src/components/dashboard/prefetch-profiles.tsx: Background prefetching
  - public/sw.js: Enhanced with stale-while-revalidate for API responses
- All quality checks pass: typecheck ✓, lint ✓, build ✓

**Learnings for future iterations:**
- React Suspense boundaries enable streaming server rendering - site info renders instantly while forecast streams in
- Service worker stale-while-revalidate: Return cached data instantly, fetch fresh in background, perfect for weather data
- requestIdleCallback for non-blocking prefetch - browser schedules work during idle time
- Separate cache buckets for different strategies: CACHE_NAME for static assets, API_CACHE_NAME for API responses
- Progressive loading pattern: Show layout/skeleton first, then data layers as they arrive
- Cache-Control headers with s-maxage for CDN caching, stale-while-revalidate for serving stale content while revalidating
- Prefetch priorities: First 3 sites immediately (most likely to be viewed), remaining sites delayed
- Service worker cache matching: Use cache.match() for instant cached responses, fetch in background for fresh data
---
