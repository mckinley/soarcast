# Ralph Progress Log
Started: $(date)
---

## Codebase Patterns
- Next.js 15 App Router with src/ directory
- shadcn/ui components installed via `npx shadcn@latest add <component>`
- Tailwind CSS v4
- JSON file storage in data/ directory at project root
- Server actions for data mutations - define in actions.ts, wrap with inline 'use server' when passing to client components
- TypeScript strict mode
- AppShell component wraps all pages in src/app/layout.tsx - provides navigation automatically
- next-themes for dark mode with suppressHydrationWarning on html tag
- revalidatePath() in server actions to refresh UI after mutations
- updateJSON takes 3 params: updateJSON(filename, defaultValue, updaterFunction) - don't forget the defaultValue!
---
## 2026-03-04 - US-001
- Implemented responsive app shell with sidebar navigation and mobile sheet menu
- Installed shadcn components: sidebar, button, sheet, separator, tooltip, input, skeleton
- Installed next-themes for dark mode support with theme toggle
- Created AppShell component that wraps all pages with navigation
- Created ThemeProvider component for dark mode functionality
- Added placeholder pages for Dashboard (/), Sites (/sites), and Settings (/settings)
- Updated metadata in layout to reflect SoarCast branding

**Files changed:**
- src/app/layout.tsx - Added ThemeProvider, TooltipProvider, and AppShell wrapper
- src/app/page.tsx - Simplified to basic Dashboard placeholder
- src/app/sites/page.tsx - New placeholder for Sites page
- src/app/settings/page.tsx - New placeholder for Settings page
- src/components/app-shell.tsx - New app shell with sidebar and mobile navigation
- src/components/theme-provider.tsx - New theme provider wrapper
- src/components/ui/* - shadcn components (button, sheet, sidebar, etc.)
- src/hooks/use-mobile.ts - Mobile detection hook
- package.json - Added next-themes dependency

**Learnings for future iterations:**
- shadcn's Sidebar component auto-installs several dependencies (separator, tooltip, input, skeleton) for full functionality
- Theme provider needs `suppressHydrationWarning` on html tag to prevent hydration mismatches
- AppShell pattern centralizes layout logic - all pages automatically get navigation without prop drilling
- Mobile navigation uses Sheet component (slide-in drawer) instead of trying to make Sidebar responsive
- ThemeToggle needs mounted state check to prevent hydration errors with theme detection
---
## 2026-03-04 - US-002
- Implemented type-safe JSON file storage layer with generic read/write/update functions
- Created comprehensive TypeScript interfaces for all data shapes (Site, Forecast, DayScore, Settings)
- Established data/ directory structure with three storage files: sites.json, forecasts.json, settings.json
- Auto-creates data directory and initializes JSON files with defaults on first access
- Implemented read-modify-write pattern in updateJSON for concurrent access safety
- Added typecheck npm script to package.json

**Files changed:**
- src/lib/storage.ts - New storage utility with readJSON, writeJSON, updateJSON, fileExists functions
- src/types/index.ts - New type definitions for all data structures
- package.json - Added typecheck script
- data/sites.json - Initial empty sites array
- data/forecasts.json - Initial empty forecasts object
- data/settings.json - Default notification settings

**Learnings for future iterations:**
- Storage pattern: readJSON auto-creates files with defaults if missing (ENOENT error handling)
- updateJSON provides atomic read-modify-write pattern - always use this for mutations to avoid race conditions
- All data types exported from src/types/index.ts for consistency across the app
- Forecast cache key pattern: `${siteId}_${date}` for efficient lookup
- Settings.sitePreferences uses Record<string, boolean> for per-site notification toggles
- data/ directory already in .gitignore to keep user data out of version control
---
## 2026-03-04 - US-003
- Implemented comprehensive site management with CRUD operations
- Created server actions in src/app/sites/actions.ts (getSites, addSite, updateSite, deleteSite)
- Built reusable SiteFormDialog component with full validation
- Built SiteCard component for displaying site details
- Updated /sites page with card grid layout, empty state, and add/edit/delete functionality
- Installed shadcn components: card, dialog, form, select, label

**Files changed:**
- src/app/sites/actions.ts - New server actions for site CRUD operations
- src/app/sites/page.tsx - Full site management UI with grid layout
- src/components/site-form-dialog.tsx - Reusable form dialog for add/edit
- src/components/site-card.tsx - Site display card with edit/delete actions
- src/components/ui/* - New shadcn components (card, dialog, form, select, label)
- package.json/package-lock.json - Updated dependencies from shadcn install

**Learnings for future iterations:**
- Server Actions pattern: define core actions in actions.ts file, wrap with inline 'use server' functions when passing to client components
- revalidatePath() in server actions ensures UI updates after mutations without manual cache invalidation
- Form validation happens client-side for immediate feedback, but server actions should also validate for security
- Wind directions stored as number array (degrees 0-360), comma-separated input for user convenience
- Confirmation dialogs (window.confirm) provide simple destructive action protection before implementing more complex modals
- Empty state with prominent CTA improves UX when no data exists yet
---
## 2026-03-04 - US-004
- Implemented weather data fetching service using Open-Meteo API
- Created src/lib/weather.ts with fetchWeatherForecast(), getForecast(), fetchAllForecasts(), and clearExpiredForecasts() functions
- Fetches 7-day hourly forecast data including all required parameters: temperature, wind speed/direction/gusts, cloud cover, CAPE, precipitation probability, pressure
- Also fetches model elevation and sunrise/sunset times for each site
- Implements 6-hour cache expiration strategy to balance freshness vs. API load
- Cache key pattern: ${siteId}_${date} stores forecasts in data/forecasts.json
- Comprehensive error handling with descriptive error messages for API failures
- TypeScript types defined for Open-Meteo API response structure

**Files changed:**
- src/lib/weather.ts - New weather service module
- prd.json - Updated US-004 passes to true

**Learnings for future iterations:**
- Open-Meteo API requires no authentication and provides free access to comprehensive weather data
- Cache strategy uses ${siteId}_${YYYY-MM-DD} pattern for daily forecasts - this allows efficient lookup and automatic daily refresh
- updateJSON requires 3 parameters (filename, defaultValue, updater function) for safe read-modify-write operations
- URLSearchParams is the cleanest way to build API query strings with multiple parameters
- CAPE (Convective Available Potential Energy) is measured in J/kg - higher values indicate stronger thermal potential
- Sunrise/sunset come from the daily object, while all weather parameters are in the hourly object
- getForecast() provides automatic cache-or-fetch logic - use this for most cases instead of direct fetchWeatherForecast()
- fetchAllForecasts() can parallelize multiple site fetches using Promise.all for efficiency
---
## 2026-03-04 - US-005
- Implemented XC soaring score algorithm that analyzes weather data to produce 0-100 scores for flying days
- Created src/lib/scoring.ts with calculateDailyScores() as main entry point
- Analyzes only flyable hours (10:00-17:00 local time) to focus on when XC flights actually happen
- Weighted scoring system: CAPE 30%, wind speed 25%, wind direction 20%, cloud cover 15%, precipitation 10%
- Each factor normalized to 0-100 scale before applying weights
- Score labels: Poor (0-30), Fair (31-50), Good (51-70), Great (71-85), Epic (86-100)
- Handles missing CAPE data gracefully (treats null as 0)

**Files changed:**
- src/lib/scoring.ts - New scoring algorithm module
- prd.json - Updated US-005 passes to true

**Learnings for future iterations:**
- CAPE scoring: Linear scale from 0 J/kg (score 0) to 2000 J/kg (score 100) - typical XC days have 500-1500 J/kg CAPE
- Wind speed scoring: Uses threshold approach (e.g., 30% of max = 100, 50% of max = 90, 85% of max = 40) to better reflect pilot preferences than pure linear
- Wind direction scoring: Must account for circular nature of compass - use angleDifference() helper that handles 359° to 1° wraparound
- Hourly data grouping: Extract date from ISO timestamp with .split('T')[0], filter by hour range before grouping
- Factor scores are rounded for UI display but weighted calculation uses full precision for accuracy
- Days with no flyable hours default to score 0 (Poor) rather than erroring
- The scoring algorithm is the single source of truth for XC potential - UI components will consume DayScore objects
---
